services:
  livo-landing:
    image: ${IMAGE_NAME:-livo-landing}:${IMAGE_TAG:-latest}
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
